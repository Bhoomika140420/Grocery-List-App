const SearchItem = () => {
    return(
        <form className='searchForm' onSubmit={(e) => e.preventDefault}>
            <label htmlFor=""></label>
        </form>
    )
}

export default SearchItem;